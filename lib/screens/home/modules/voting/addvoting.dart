import 'package:flutter/material.dart';

class AddVoting extends StatelessWidget {
  static const String id = 'add_voting';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
